


// Approximate the square root of x
var x: float;
var guess: float;
var iter: int;
read(x);
while (iter > 0) {
var quot: float = x / guess;
guess = 0.5 * (guess + quot);
iter = iter - 1;
}
print(guess);
print(guess * guess);