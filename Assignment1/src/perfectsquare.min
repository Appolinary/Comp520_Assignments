


// Approximate the square root of x
var x: float = 0.0;
var guess: float = 0.0;
var iter: int = 9;
read(iter);
while (iter > 0) {
var quot: float = x / guess;
guess = 0.5 * (guess + quot);
iter = iter - 1;
}
print(guess);
print(guess * guess);