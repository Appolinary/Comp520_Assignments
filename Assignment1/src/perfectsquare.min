


// Approximate the square root of x
var x: float = 89.0;
var guess: float = 1.0;
var iter: int = 10;
var isGoing: boolean;

guess = 10 + 10.0;
var name : string = "fab";

read(isGoing);
while (iter > 0) {
	var quot: float = x / guess;
	guess = 0.5 * (guess + quot);
	iter = iter - 1;
	print(isGoing);
   

	var guess1 : float;
	var guess2: int;
	var guess3 : boolean;
	var guess4 : string;
	
	var guess5: float;
	var guess6 : int;
}
print(guess);
print(guess * guess);
